[project]
name = "certbot-dns-champ"
authors = [
  { name = "statuary", email = "hi@toomanybees.com" },
]
description = ""
readme = "README.md"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = [
  "certbot>=5.4.0",
]

[project.entry-points."certbot.plugins"]
dns-champ = "champ:Authenticator"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
